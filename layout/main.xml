<vc>
    <body>
        <pad id="pot-root">
            <bind:collection bind="additions"> <!-- bind a repetitive collection of CoffeeAdditions -->
                <vc bind="__items"> <!-- elements will be placed inside this <vc> -->
                    <label style="bold" text="Available additions:" />
                </vc>
                <bind:template> <!-- an element template -->
                    <checkbox bind:value="selected" bind:text="name" />
                </bind:template>
            </bind:collection>
        </pad>
    </body>

    <hc>
        <button id="brew" icon="arrow-right" text="Brew" />
        <button id="retrieve" icon="coffee" text="Retrieve" />
        <button id="refresh" icon="refresh" text="Refresh" />
    </hc>
</vc>